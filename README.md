# Abbreviation-llm
Create a web app that accepts questions and optional documents (in plain text, PDF, Word, or HTML formats), and uses an open-source LLM to answer the question using the uploaded document as context
